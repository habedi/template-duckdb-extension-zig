name: Build and Test Extensions

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  EXTENSION_NAME: extension
  EXTENSION_API_VERSION: v1.2.0
  EXTENSION_VERSION: v0.1.0
  ZIG_VERSION: 0.15.2
  DUCKDB_VERSION: v1.4.1

jobs:
  build-linux-amd64:
    name: Build Linux AMD64
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build extension
        run: |
          make build-linux-amd64 \
            EXTENSION_NAME=${{ env.EXTENSION_NAME }} \
            EXTENSION_API_VERSION=${{ env.EXTENSION_API_VERSION }} \
            EXTENSION_VERSION=${{ env.EXTENSION_VERSION }}

      - name: Download DuckDB CLI
        run: |
          wget -q https://github.com/duckdb/duckdb/releases/download/${{ env.DUCKDB_VERSION }}/duckdb_cli-linux-amd64.zip
          unzip -q duckdb_cli-linux-amd64.zip
          chmod +x duckdb

      - name: Smoke test
        run: |
          ./duckdb -unsigned -c "
            LOAD 'zig-out/lib/linux_amd64/extension.duckdb_extension';
            SELECT extension_name, loaded, extension_version FROM duckdb_extensions() WHERE extension_name = '${{ env.EXTENSION_NAME }}';
          " > test_output.txt
          cat test_output.txt
          grep -q "${{ env.EXTENSION_NAME }}" test_output.txt && grep -q "true" test_output.txt && echo "Smoke test passed: Extension loaded successfully" || (echo "Smoke test failed" && exit 1)

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-linux-amd64
          path: zig-out/lib/linux_amd64/extension.duckdb_extension
          if-no-files-found: error

  build-linux-arm64:
    name: Build Linux ARM64
    runs-on: ubuntu-24.04-arm64
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build extension
        run: |
          make build-linux-arm64 \
            EXTENSION_NAME=${{ env.EXTENSION_NAME }} \
            EXTENSION_API_VERSION=${{ env.EXTENSION_API_VERSION }} \
            EXTENSION_VERSION=${{ env.EXTENSION_VERSION }}

      - name: Download DuckDB CLI
        run: |
          wget -q https://github.com/duckdb/duckdb/releases/download/${{ env.DUCKDB_VERSION }}/duckdb_cli-linux-arm64.zip
          unzip -q duckdb_cli-linux-arm64.zip
          chmod +x duckdb

      - name: Smoke test
        run: |
          ./duckdb -unsigned -c "
            LOAD 'zig-out/lib/linux_arm64/extension.duckdb_extension';
            SELECT extension_name, loaded, extension_version FROM duckdb_extensions() WHERE extension_name = '${{ env.EXTENSION_NAME }}';
          " > test_output.txt
          cat test_output.txt
          grep -q "${{ env.EXTENSION_NAME }}" test_output.txt && grep -q "true" test_output.txt && echo "Smoke test passed: Extension loaded successfully" || (echo "Smoke test failed" && exit 1)

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-linux-arm64
          path: zig-out/lib/linux_arm64/extension.duckdb_extension
          if-no-files-found: error

  build-linux-amd64-musl:
    name: Build Linux AMD64 (musl)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build extension
        run: |
          make build-linux-amd64-musl \
            EXTENSION_NAME=${{ env.EXTENSION_NAME }} \
            EXTENSION_API_VERSION=${{ env.EXTENSION_API_VERSION }} \
            EXTENSION_VERSION=${{ env.EXTENSION_VERSION }}

      - name: Verify build artifact
        run: |
          if [ -f "zig-out/lib/linux_amd64_musl/extension.duckdb_extension" ]; then
            echo "Extension built successfully"
            file zig-out/lib/linux_amd64_musl/extension.duckdb_extension
            ls -lh zig-out/lib/linux_amd64_musl/extension.duckdb_extension
          else
            echo "Extension file not found"
            exit 1
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-linux-amd64-musl
          path: zig-out/lib/linux_amd64_musl/extension.duckdb_extension
          if-no-files-found: error

  build-linux-arm64-musl:
    name: Build Linux ARM64 (musl)
    runs-on: ubuntu-24.04-arm64
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build extension
        run: |
          make build-linux-arm64-musl \
            EXTENSION_NAME=${{ env.EXTENSION_NAME }} \
            EXTENSION_API_VERSION=${{ env.EXTENSION_API_VERSION }} \
            EXTENSION_VERSION=${{ env.EXTENSION_VERSION }}

      - name: Verify build artifact
        run: |
          if [ -f "zig-out/lib/linux_arm64_musl/extension.duckdb_extension" ]; then
            echo "Extension built successfully"
            file zig-out/lib/linux_arm64_musl/extension.duckdb_extension
            ls -lh zig-out/lib/linux_arm64_musl/extension.duckdb_extension
          else
            echo "Extension file not found"
            exit 1
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-linux-arm64-musl
          path: zig-out/lib/linux_arm64_musl/extension.duckdb_extension
          if-no-files-found: error

  build-macos-amd64:
    name: Build macOS AMD64
    runs-on: macos-13
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build extension
        run: |
          make build-macos-amd64 \
            EXTENSION_NAME=${{ env.EXTENSION_NAME }} \
            EXTENSION_API_VERSION=${{ env.EXTENSION_API_VERSION }} \
            EXTENSION_VERSION=${{ env.EXTENSION_VERSION }}

      - name: Download DuckDB CLI
        run: |
          wget -q https://github.com/duckdb/duckdb/releases/download/${{ env.DUCKDB_VERSION }}/duckdb_cli-osx-universal.zip
          unzip -q duckdb_cli-osx-universal.zip
          chmod +x duckdb

      - name: Smoke test
        run: |
          ./duckdb -unsigned -c "
            LOAD 'zig-out/lib/osx_amd64/extension.duckdb_extension';
            SELECT extension_name, loaded, extension_version FROM duckdb_extensions() WHERE extension_name = '${{ env.EXTENSION_NAME }}';
          " > test_output.txt
          cat test_output.txt
          grep -q "${{ env.EXTENSION_NAME }}" test_output.txt && grep -q "true" test_output.txt && echo "Smoke test passed: Extension loaded successfully" || (echo "Smoke test failed" && exit 1)

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-macos-amd64
          path: zig-out/lib/osx_amd64/extension.duckdb_extension
          if-no-files-found: error

  build-macos-arm64:
    name: Build macOS ARM64
    runs-on: macos-14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build extension
        run: |
          make build-macos-arm64 \
            EXTENSION_NAME=${{ env.EXTENSION_NAME }} \
            EXTENSION_API_VERSION=${{ env.EXTENSION_API_VERSION }} \
            EXTENSION_VERSION=${{ env.EXTENSION_VERSION }}

      - name: Download DuckDB CLI
        run: |
          wget -q https://github.com/duckdb/duckdb/releases/download/${{ env.DUCKDB_VERSION }}/duckdb_cli-osx-universal.zip
          unzip -q duckdb_cli-osx-universal.zip
          chmod +x duckdb

      - name: Smoke test
        run: |
          ./duckdb -unsigned -c "
            LOAD 'zig-out/lib/osx_arm64/extension.duckdb_extension';
            SELECT extension_name, loaded, extension_version FROM duckdb_extensions() WHERE extension_name = '${{ env.EXTENSION_NAME }}';
          " > test_output.txt
          cat test_output.txt
          grep -q "${{ env.EXTENSION_NAME }}" test_output.txt && grep -q "true" test_output.txt && echo "Smoke test passed: Extension loaded successfully" || (echo "Smoke test failed" && exit 1)

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-macos-arm64
          path: zig-out/lib/osx_arm64/extension.duckdb_extension
          if-no-files-found: error

  build-windows-amd64:
    name: Build Windows AMD64
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build extension
        shell: bash
        run: |
          make build-windows-amd64 \
            EXTENSION_NAME=${{ env.EXTENSION_NAME }} \
            EXTENSION_API_VERSION=${{ env.EXTENSION_API_VERSION }} \
            EXTENSION_VERSION=${{ env.EXTENSION_VERSION }}

      - name: Download DuckDB CLI
        shell: bash
        run: |
          curl -L -o duckdb_cli-windows-amd64.zip https://github.com/duckdb/duckdb/releases/download/${{ env.DUCKDB_VERSION }}/duckdb_cli-windows-amd64.zip
          unzip -q duckdb_cli-windows-amd64.zip

      - name: Smoke test
        shell: bash
        run: |
          ./duckdb.exe -unsigned -c "
            LOAD 'zig-out/lib/windows_amd64/extension.duckdb_extension';
            SELECT extension_name, loaded, extension_version FROM duckdb_extensions() WHERE extension_name = '${{ env.EXTENSION_NAME }}';
          " > test_output.txt
          cat test_output.txt
          grep -q "${{ env.EXTENSION_NAME }}" test_output.txt && grep -q "true" test_output.txt && echo "Smoke test passed: Extension loaded successfully" || (echo "Smoke test failed" && exit 1)

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-windows-amd64
          path: zig-out/lib/windows_amd64/extension.duckdb_extension
          if-no-files-found: error

  build-windows-arm64:
    name: Build Windows ARM64
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build extension
        shell: bash
        run: |
          make build-windows-arm64 \
            EXTENSION_NAME=${{ env.EXTENSION_NAME }} \
            EXTENSION_API_VERSION=${{ env.EXTENSION_API_VERSION }} \
            EXTENSION_VERSION=${{ env.EXTENSION_VERSION }}

      - name: Verify build artifact
        shell: bash
        run: |
          if [ -f "zig-out/lib/windows_arm64/extension.duckdb_extension" ]; then
            echo "Extension built successfully for Windows ARM64"
            file zig-out/lib/windows_arm64/extension.duckdb_extension || echo "file command not available"
            ls -lh zig-out/lib/windows_arm64/extension.duckdb_extension
          else
            echo "Extension file not found"
            exit 1
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-windows-arm64
          path: zig-out/lib/windows_arm64/extension.duckdb_extension
          if-no-files-found: error

  summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs:
      - build-linux-amd64
      - build-linux-arm64
      - build-linux-amd64-musl
      - build-linux-arm64-musl
      - build-macos-amd64
      - build-macos-arm64
      - build-windows-amd64
      - build-windows-arm64
    if: always()
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display summary
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Artifacts built:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          for dir in artifacts/*/; do
            platform=$(basename "$dir")
            echo "- **${platform}**" >> $GITHUB_STEP_SUMMARY
            ls -lh "$dir" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Extension Details" >> $GITHUB_STEP_SUMMARY
          echo "- **Extension API Version:** ${{ env.EXTENSION_API_VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Extension Version:** ${{ env.EXTENSION_VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Zig Version:** ${{ env.ZIG_VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **DuckDB Version (tested):** ${{ env.DUCKDB_VERSION }}" >> $GITHUB_STEP_SUMMARY
